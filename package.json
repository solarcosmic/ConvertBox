{
  "name": "convertbox",
  "version": "1.0.0",
  "description": "Convert almost anything entirely within your browser.",
  "main": "index.js",
  "scripts": {
    "build": "webpack --mode production",
    "watch": "webpack --watch --mode development",
    "build:chrome": "cp manifest.json dist/manifest.json && cp -r src dist/",
    "build:firefox": "cp manifest.firefox.json dist/manifest.json && cp -r src dist/"
  },
  "author": "solarcosmic",
  "license": "MIT",
  "devDependencies": {
    "copy-webpack-plugin": "^13.0.0",
    "qrcode": "^1.5.4",
    "webpack": "^5.99.9",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "hex-rgb": "^5.0.0",
    "marked": "^15.0.12",
    "turndown": "^7.2.0"
  }
}
